[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    live: hits the live OpenRouter API (skipped in CI by default)
    integration: requires docker-compose services to be running
asyncio_mode = auto
